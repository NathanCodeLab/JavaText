Collection: Framework: Interface:

It supports Dissimilar datatype
No Memory wastage since there is no fixed memory. Memory will be allocated at the run time

List - Interface. - java.util
Set
Map

List li = new List(); ---> Wont Work

ArrayList
LinkedList
Vector

List li = new ArrayList();

List
====

List is index based one.
List allows duplicate.
List supports both Normal for loop and Enhanced For Loop

methods in List
===============

li.add(30);
li.size();
li.get(index);
li.add(index, Value);
li.remove(index);
li.set(index, Value);
li.indexOf(Value);
li.lastIndexOf(Value);
li.contains(Value);

li.addAll(li1);
li.retainAll(li1);
li.removeAll(li1);


Generics: List<Integer> li = new ArrayList<Integer>();
======================================================

